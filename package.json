{
  "name": "adaptivenet",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Interactive demos of node–edge dynamical systems.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 8000",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
